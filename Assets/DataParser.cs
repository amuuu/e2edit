/*
static 

    static readonly byte[] PatternDumpRequest =
      { 0xF0, 0x42, 0x30, 0x00, 0x01, 0x23, 0x10, 0xF7 };
        var name = "";
        for (var i = 16; i < 34; i++)
        {
            if (buffer[i] == 0) break;
            name += (char)buffer[i];
        }
        Debug.Log(name);
        */
